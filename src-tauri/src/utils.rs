//! Re-export of core's utilities.
//!
//! Kept so the shell modules still awaiting the lift address these as
//! `crate::utils::*` — the same path they will use once they live in core.

pub use ltk_manager_core::utils::*;
