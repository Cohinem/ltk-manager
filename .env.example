# Copy to .env.local to turn on. Dev-only flags - a production build ignores them.

# Seed a stand-in update so the titlebar Update cell and the changelog dialog
# can be exercised without cutting a release.
VITE_MOCK_UPDATE=1
