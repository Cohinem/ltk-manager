
### Added

- copy whole objects as declarations
- copy declarations and declare references
- draw apply diagnostics on declared rows
- declare game bin container edits
- declare game bin leaf edits
- restore manifest text
- add rendered value entry point
- add Reveal in Files to file tabs
- open map files on the map
- sort file objects by class
- locate hash-named layer files
- outline map chunks
- preview map classes
- idle map structures and props
- draw the sky behind a map backdrop
- draw map particles and structures
- stand the subject on the backdrop and move it by a gizmo
- group backdrop maps by their skins
- pick the backdrop map from the install
- draw a map backdrop with its own materials
- read the blend factor pair and route cutouts
- draw a map backdrop behind a preview
- encode a map as one LTKM buffer

### Documentation

- format release notes

### Fixed

- sample a pose at the graph's tick
- scroll a shared skin texture from the clock
- mount and bind the map scene once
- drop stale placements on a game index rebuild
- stop caching failed map preview reads
- keep map textures streaming past a stalled read
- hold the reader's view through camera moves
- tie the subject's placement to its map
- keep the placement gizmo's turn
- stand the subject on the map's dense ground
- brighten map tints and rebind backdrop textures in place
- give the Viewport test a query client

### Other

- pin league-mod releases
- pin league-toolkit releases
- bump league-toolkit and league-mod
- bump ltk_mesh to 0.5.0

### Performance

- size the wad mount cache for a map
- pace the backdrop texture waves

### Refactored

- lift map into own crate
- name the decoded map mesh record

